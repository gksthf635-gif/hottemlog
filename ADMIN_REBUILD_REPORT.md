# 관리자 UX 리빌딩 v2

- 통합 경로: `/admin/contents`, `/admin/contents/new`, `/admin/contents/[id]/edit`.
- 한 화면에서 영상과 상품 1~100개 저장. 기존 상품 검색·재사용·편집, 관계 해제 지원.
- 필수: 플랫폼, 영상 URL, 영상 제목, 상품명, 쿠팡파트너스 링크. 나머지 입력은 선택.
- 메뉴: 대시보드, 콘텐츠 관리, 통계, 설정, 로그아웃. 카테고리와 기존 페이지는 보조 관리로 보존.
- DB 테이블 구조·기존 slug·숨긴 설명/추천/태그·클릭 기록 보존. 콘텐츠 삭제 시 상품 자동 삭제 없음.
- 추가 migration: `202609110001_content_editor.sql`. 관리자 전용 트랜잭션 저장과 공개 상태 변경 함수. 부분 저장 실패는 전체 롤백.
- 공개 카드와 상세의 빈 한줄평·설명·추천·카테고리 표시 제거.
- 모바일: 영상 → 상품 블록 → 상품 추가 → 하단 고정 저장 버튼. 업로드 중 저장 방지.
- `/go` 클릭 기록과 Analytics 구조 유지.
- 검증: npm install, lint, build 성공. 단위 5개 / DB 13개 통과. DB 검사에는 최소 입력, 다중 상품, 기존 상품 재사용, 부분 실패 롤백, 삭제 시 보존, 비관리자 차단 포함.
- 제한: 외부 API 자동수집 없음. 기존 상품 편집은 다른 연결 영상에도 적용. 실제 휴대폰 실기기 검사는 별도.

운영 반영: Supabase SQL Editor에서 추가 migration 적용 성공. GitHub main과 Vercel 자동 배포에 반영. 실제 관리자 브라우저에서 필수값만 입력한 비공개 저장 → 성공 메시지/목록 이동 → 삭제 시 상품 보존까지 확인 후 테스트 데이터 정리 완료.

## 접속 안정성 및 자동 이미지 보완

- Vercel 로그에서 홈페이지 인기 콘텐츠 RPC의 `Gateway Timeout`을 확인. 환경변수와 기존 함수는 정상 등록 상태.
- 일시적 조회 실패는 1회 재시도. 인기 순위 실패는 기본 콘텐츠 순서로 표시하여 홈페이지 전체 장애를 방지.
- YouTube URL로 기본 썸네일 자동 적용. 기존 고해상도 썸네일 실패 시 기본 이미지로 재시도.
- 쿠팡/Instagram 공개 메타데이터 이미지 조회 추가. 관리자 인증 및 허용 도메인/리디렉션 검증, 시간·응답크기 제한 적용.
- 쿠팡 이미지가 제공되지 않으면 영상 썸네일을 상품 이미지로 사용하며 화면에 구분 안내. Instagram 공개 메타데이터가 차단되면 직접 업로드 필요.
- 쿠팡 링크 입력은 고정 https:// 접두어와 전체 링크 붙여넣기 모두 지원.
- 이번 변경은 추가 migration 없음. 단위 테스트 8개, lint, build 통과.
